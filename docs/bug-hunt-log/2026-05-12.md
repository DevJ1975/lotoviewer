# Bug Hunt — 2026-05-12

Auto-generated by `scripts/bug-hunt/run.mjs`. Format spec:
`docs/bug-hunt-log/README.md`.

## Checks

| Check | Status |
|---|---|
| lint | — skipped |
| typecheck | — skipped |
| tests | — skipped |

## Pattern findings

| Pattern | Severity | Hits |
|---|---|---|
| loto-equipment-tenant-scope | high | 11 |
| unvalidated-atob | high | 1 |
| admin-delete-tenant-scope | high | 7 |
| modal-missing-dialog-role | medium | 19 |
| deferred-revoke-object-url | low | 10 |
| non-null-env | low | 2 |
| void-dead-code | low | 9 |

### loto-equipment-tenant-scope — loto_equipment query missing tenant_id filter
Severity: **high** · Hits: **11**

- `apps/web/app/decommission/page.tsx:121` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `apps/web/app/departments/[dept]/page.tsx:72` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  const { error: patchErr } = await supabase.from('loto_equipment')
  ```
- `apps/web/app/equipment/[id]/page.tsx:60` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `apps/web/app/equipment/[id]/page.tsx:119` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  const { data } = await supabase.from('loto_equipment').select('*').eq('equipment_id', equipmentId).single()
  ```
- `apps/web/app/equipment/[id]/page.tsx:215` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `apps/web/app/equipment/[id]/page.tsx:255` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `apps/web/components/SpanishTranslationSheet.tsx:45` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `apps/web/components/dashboard/PlacardDetailPanel.tsx:45` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `apps/web/components/placard/PlacardDetailsSheet.tsx:52` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `apps/web/components/placard/PlacardPdfPreview.tsx:90` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```
- `packages/core/src/queries/equipment.ts:47` — loto_equipment query filters by equipment_id without tenant_id — equipment_id is not globally unique.
  ```
  .from('loto_equipment')
  ```

### unvalidated-atob — atob() call without data-URL or length validation
Severity: **high** · Hits: **1**

- `apps/web/lib/pdfShared.ts:150` — atob() call has no preceding data-URL or length guard; an empty/malformed input will throw opaquely.
  ```
  const binary = atob(base64)
  ```

### admin-delete-tenant-scope — Service-role delete by id without tenant_id filter
Severity: **high** · Hits: **7**

- `apps/web/app/api/risk/route.ts:300` — Service-role .delete() filters by id without tenant_id — verify the id is sourced from a tenant-scoped read.
  ```
  await admin.from('risks').delete().eq('id', created.id)
  ```
- `apps/web/app/api/superadmin/knowledge/seed-regulations/route.ts:206` — Service-role .delete() filters by id without tenant_id — verify the id is sourced from a tenant-scoped read.
  ```
  await admin.from('knowledge_documents').delete().eq('id', doc.id)
  ```
- `apps/web/app/api/superadmin/policies/[id]/route.ts:21` — Service-role .delete() filters by id without tenant_id — verify the id is sourced from a tenant-scoped read.
  ```
  const { error } = await admin.from('knowledge_documents').delete().eq('id', id)
  ```
- `apps/web/app/api/superadmin/policies/upload/route.ts:266` — Service-role .delete() filters by id without tenant_id — verify the id is sourced from a tenant-scoped read.
  ```
  await admin.from('knowledge_documents').delete().eq('id', doc.id)
  ```
- `apps/web/app/api/superadmin/queries/[id]/route.ts:75` — Service-role .delete() filters by id without tenant_id — verify the id is sourced from a tenant-scoped read.
  ```
  const { error } = await admin.from('saved_queries').delete().eq('id', idNum)
  ```
- `apps/web/app/api/superadmin/release-notes/[id]/route.ts:53` — Service-role .delete() filters by id without tenant_id — verify the id is sourced from a tenant-scoped read.
  ```
  const { error } = await admin.from('release_notes').delete().eq('id', idNum)
  ```
- `apps/web/lib/ai/syncManualToRag.ts:182` — Service-role .delete() filters by id without tenant_id — verify the id is sourced from a tenant-scoped read.
  ```
  await admin.from('knowledge_documents').delete().eq('id', doc.id)
  ```

### modal-missing-dialog-role — Modal container missing role="dialog" / aria-modal
Severity: **medium** · Hits: **19**

- `apps/web/app/admin/loto-devices/_components/CheckoutDialog.tsx:321` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40">
  ```
- `apps/web/app/admin/training-records/page.tsx:227` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40 overflow-y-auto py-10">
  ```
- `apps/web/app/admin/webhooks/page.tsx:263` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40 overflow-y-auto py-10">
  ```
- `apps/web/app/confined-spaces/[id]/page.tsx:391` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40 overflow-y-auto py-10">
  ```
- `apps/web/app/confined-spaces/[id]/permits/[permitId]/_components/CancelDialog.tsx:71` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40">
  ```
- `apps/web/app/confined-spaces/[id]/permits/[permitId]/_components/EditRosterDialog.tsx:70` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40 overflow-y-auto py-10">
  ```
- `apps/web/app/confined-spaces/page.tsx:291` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40">
  ```
- `apps/web/app/hot-work/[permitId]/_components/CancelDialog.tsx:59` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40">
  ```
- `apps/web/app/hot-work/[permitId]/_components/FireWatchSignOnDialog.tsx:44` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex items-center justify-center px-4 bg-black/40">
  ```
- `apps/web/app/incidents/qr/page.tsx:636` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 bg-black/40 flex items-center justify-center p-4" onClick={onClose}>
  ```
- `apps/web/components/AnnotatedPhoto.tsx:259` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 bg-black/70 flex flex-col">
  ```
- `apps/web/components/BatchPrintModal.tsx:125` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  className="fixed inset-0 z-50 bg-black/40 backdrop-blur-sm flex items-center justify-center p-4"
  ```
- `apps/web/components/RenameDepartmentModal.tsx:46` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/40 backdrop-blur-sm"
  ```
- `apps/web/components/equipment/AddEquipmentDialog.tsx:155` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/40 backdrop-blur-sm"
  ```
- `apps/web/components/placard/PlacardPdfPreview.tsx:151` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 bg-black/60 flex flex-col">
  ```
- `apps/web/components/ui/alert-dialog.tsx:33` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  "fixed inset-0 z-50 bg-black/50 backdrop-blur-sm transition-[opacity] duration-200 data-[ending-style]:opacity-0 data-[starting-style]:opacity-0",
  ```
- `apps/web/components/ui/dialog.tsx:30` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  "fixed inset-0 z-50 bg-black/50 backdrop-blur-sm transition-[opacity] duration-200 data-[ending-style]:opacity-0 data-[starting-style]:opacity-0",
  ```
- `apps/web/components/ui/drawer.tsx:45` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  className={cn("fixed inset-0 z-50 bg-black/50", className)}
  ```
- `apps/web/components/ui/sheet.tsx:44` — Modal-shaped container has no role="dialog" + aria-modal — screen readers will not announce it.
  ```
  <div className="fixed inset-0 z-50 flex justify-end" onMouseDown={e => { if (e.target === e.currentTarget) onClose() }}>
  ```

### deferred-revoke-object-url — URL.revokeObjectURL deferred via setTimeout
Severity: **low** · Hits: **10**

- `apps/web/app/admin/audit/page.tsx:238` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 60_000)
  ```
- `apps/web/app/admin/compliance-bundle/page.tsx:150` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 60_000)
  ```
- `apps/web/app/admin/hygiene-log/page.tsx:302` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 60_000)
  ```
- `apps/web/app/chemicals/[id]/_components/PrintLabelPanel.tsx:131` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 60_000)
  ```
- `apps/web/app/chemicals/tier-two/page.tsx:98` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 60_000)
  ```
- `apps/web/app/confined-spaces/[id]/permits/[permitId]/page.tsx:212` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 60_000)
  ```
- `apps/web/app/equipment-readiness/page.tsx:121` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 1000)
  ```
- `apps/web/app/hot-work/[permitId]/page.tsx:211` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 60_000)
  ```
- `apps/web/app/incidents/qr/page.tsx:855` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(obj), 1000)
  ```
- `apps/web/components/safetyBoards/ExportPdfButton.tsx:45` — URL.revokeObjectURL deferred via setTimeout — keeps the blob alive after unmount. Revoke synchronously after click() (or queueMicrotask).
  ```
  setTimeout(() => URL.revokeObjectURL(url), 1000)
  ```

### non-null-env — process.env.X! non-null assertion
Severity: **low** · Hits: **2**

- `apps/web/app/api/admin/users/route.ts:13` — process.env.X! non-null assertion masks a misconfigured deploy until first call.
  ```
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL!
  ```
- `apps/web/app/api/admin/users/route.ts:14` — process.env.X! non-null assertion masks a misconfigured deploy until first call.
  ```
  const anon = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  ```

### void-dead-code — `void <ident>` dead-code marker
Severity: **low** · Hits: **9**

- `apps/web/app/api/incidents/route.ts:372` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void triggeredByEmail
  ```
- `apps/web/app/manuals/[moduleId]/changelog/page.tsx:84` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void before
  ```
- `apps/web/app/superadmin/tenants/[number]/_components/EditTenantForm.tsx:61` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void _drop
  ```
- `apps/web/app/superadmin/tenants/[number]/_components/EditTenantForm.tsx:87` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void _drop
  ```
- `apps/web/app/superadmin/tenants/[number]/_components/EditTenantForm.tsx:154` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void _drop
  ```
- `apps/web/components/TenantHeaderPill.tsx:72` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void supabase
  ```
- `apps/web/components/TenantProvider.tsx:248` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void supabase
  ```
- `apps/web/components/departments/ClientReviewPanel.tsx:91` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void supabase
  ```
- `apps/web/lib/pdfShared.ts:328` — `void <ident>` with no other statements — either implement the logic or remove the marker.
  ```
  void font
  ```

---
commit: `01d53a81fceee07ec894a160f3d61ae3b9866fd4` · node: `v22.22.2`
