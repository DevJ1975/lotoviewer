// Re-export shim — implementation moved to packages/core/src/confinedSpaceLabels.ts in Phase 1.
// Kept so existing '@/lib/confinedSpaceLabels' imports keep working without a codemod.
export * from '@soteria/core/confinedSpaceLabels'
