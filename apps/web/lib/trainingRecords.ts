// Re-export shim — implementation lives at packages/core/src/trainingRecords.ts
// so the mobile app + future packages can use the same helpers without an
// app-internal import.
export * from '@soteria/core/trainingRecords'
