// Re-export shim — implementation lives in
// packages/core/src/nearMissMetrics.ts. Mirrors the riskMetrics
// shim so consumers can use '@/lib/nearMissMetrics' identically.
export * from '@soteria/core/nearMissMetrics'
