// Re-export shim — implementation moved to packages/core/src/orgConfig.ts in Phase 1.
// Kept so existing '@/lib/orgConfig' imports keep working without a codemod.
export * from '@soteria/core/orgConfig'
