// Placeholder for Supabase-generated types.
//
// This file is committed as a stub so importers compile in environments
// (CI without DB access, fresh clones) where `supabase gen types` hasn't
// been run yet. Once you run the codegen command, this file is
// overwritten with the full schema → TS types and the supabase client
// in lib/supabase.ts becomes type-narrowed.
//
// Generation:
//   1. Install the Supabase CLI: https://supabase.com/docs/guides/cli
//   2. `supabase login`
//   3. `SUPABASE_PROJECT_ID=<your-project-ref> npm run db:types`
//
// The project ref is the random-looking string in your project URL —
// e.g. https://app.supabase.com/project/<this-bit>. Once written,
// commit lib/database.types.ts so the rest of the team gets the
// narrowed types automatically.
//
// Re-run after every migration so the types track the live schema.
// A pre-commit hook or a CI step is the usual place; we don't gate
// here so a developer can iterate on a migration locally without the
// codegen step blocking them.

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Database {
  // Filled in by `supabase gen types typescript`.
  // The empty interface is intentional — see file header.
}
