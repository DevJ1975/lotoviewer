// Re-export shim — implementation moved to packages/core/src/database.types.ts in Phase 1.
// Kept so existing '@/lib/database.types' imports keep working without a codemod.
export * from '@soteria/core/database.types'
