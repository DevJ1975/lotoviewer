// Re-export shim — implementation moved to packages/core/src/features.ts in Phase 1.
// Kept so existing '@/lib/features' imports keep working without a codemod.
export * from '@soteria/core/features'
