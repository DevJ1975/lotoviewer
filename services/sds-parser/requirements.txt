# Runtime dependencies for the SDS parser service.
# The pure parser (app/parser.py) and its tests use only the stdlib; these are
# for the FastAPI service, PDF extraction, URL fetch, and Supabase staging.
fastapi>=0.115,<0.120
uvicorn[standard]>=0.30,<0.36
pdfminer.six>=20240706
python-multipart>=0.0.9        # UploadFile / multipart form parsing
httpx>=0.27,<0.29              # /parse/url
pydantic>=2.7,<3
supabase>=2.6,<3               # optional: only needed for /parse/stage
